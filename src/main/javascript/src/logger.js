/*global ActiveXObject:false */

(function(window) {

    var GL = {};

    GL._errors = [];

    GL.handleError = function(message, url, line){
    
    };

    window.error = GL.handleError;

}(this));
